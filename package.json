{
  "name": "paragon-v3-tool-chain-algorithms",
  "version": "1.0.0",
  "description": "Paragon V3 Tool-Chain Algorithms (PTA) — the sibling enforcement machinery for tool execution + model action policing. Vendors the 12 enforcement microstructures into a standalone boilerplate.",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./core": "./core/index.ts",
    "./capture": "./capture/index.ts",
    "./actuation": "./actuation/index.ts",
    "./config": "./config/index.ts",
    "./hooks": "./hooks/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build index.ts --outdir dist --target bun --format esm"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/bun": "^1.1.0"
  }
}
